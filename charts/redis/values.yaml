# Redis local development values
# Bitnami Redis 25.3.2
# Values are nested under the dependency name (redis).

redis:
  architecture: standalone

  auth:
    enabled: true
    password: "mathtrail"

  master:
    persistence:
      enabled: true
      size: 1Gi
    resourcesPreset: "nano"
